apiVersion: v1
kind: Service
metadata:
  name: memu
  labels:
    app.kubernetes.io/name: memu
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: memu
  ports:
    - name: http
      port: 80
      targetPort: http
